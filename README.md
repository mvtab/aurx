# aur-functions
.bashrc functions for the Archlinux User Repository (AUR) packages.

# Usage
```bash
cat .bashrc_functions >> ${HOME}/.bashrc
source ${HOME}/.bashrc
aur-install <package>
aur-update
```

